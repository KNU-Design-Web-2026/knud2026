# 2026-09-10 디자이너 QA

## 기준

- 기준 배포: `cbc8a73` (`QA 9.7 & favicon 추가`)
- 대상 화면: About, Work, Space
- 상태: 구현 후 검토 요청 대기. QA 항목을 자동 완료 처리하지 않는다.

## QA-001 — About 소개 문장의 단어 중간 줄바꿈

- 재현: `/about`, 1020 × 1370
- 원인: 소개 문단의 `overflow-wrap: anywhere`가 한글 단어를 음절 단위로 분리했다.
- 결정: 모든 반응형 구간의 소개 문단에 `word-break: keep-all`과 `overflow-wrap: break-word`를 함께 사용한다.
- 결과: `더 큰 가 / 능성`처럼 단어 중간에서 끊기던 문장이 공백을 기준으로 줄바꿈된다.

## QA-002 — 1020px Space 이름 클릭 시 상세 이동

- 재현: `/space`, 1020 × 1370
- 원인: 821px 초과 지도 이름이 미리보기 전용 버튼이어서 클릭해도 상세 경로로 이동하지 않았다.
- 결정: 지도 이름을 임시 배정된 작품의 `/work/[id]` 링크로 제공한다.
- 반응형 동작:
  - 1350px 이하: 클릭하면 작품 상세 페이지로 이동한다.
  - 1350px 초과: 기존 커서 hover·focus·클릭 미리보기를 유지한다.
  - 821px 이하: 기존 세로·가로 compact 지도에서 터치 링크를 유지한다.

## QA-003 — 1020px Space 안내 문구

- 재현: `/space`, 1020 × 1370
- 원인: 실제 조작은 클릭이어야 하지만 데스크톱용 커서 hover 안내가 노출됐다.
- 결정:
  - 1350px 초과: `이름에 커서를 올려 작품 정보를 확인해보세요!`
  - 822px–1350px: `이름을 클릭하여 작품 정보를 확인해보세요!`
  - 821px 이하: `이름을 터치하여 작품 정보를 확인해보세요!`

## QA-004 — About·Work·Space의 헤더·푸터 인접 여백 통일

- 재현: `/work`, 1920 × 1340 및 Web부터 Mobile까지 전체 반응형 구간
- 원인:
  - Work는 600px 이하에서 페이지 전용 `40px`·`24px` 여백이 Profile 토큰을 덮었다.
  - Space는 화면 폭에 따라 `clamp()`로 최대 `200px`까지 늘어나는 독립 여백을 사용했다.
  - About은 첫 소개 프레임의 절대 위치와 마지막 팀 섹션의 하단 여백이 화면별 고정값으로 분리돼 있었다.
- 결정: 세 페이지 모두 Profile의 `--profile-page-top-gap`에 카드 hover 확장 영역을 고려한 `20px` 시각 보정값을 더한 `--content-page-edge-gap`을 사용한다.
- About 보정: 소개 프레임만 위로 옮겨 내부 구성 간격이 달라지지 않도록, 기존 첫 콘텐츠 위치와 공통 토큰의 차이만큼 소개 섹션 전체 높이도 함께 줄인다.
- 반응형 기준값:
  - 1920px: 위·아래 `100px`
  - 1020px: 위·아래 `80px`
  - 600px: 위·아래 `80px`
  - 400px: 위·아래 `60px`

## 검증

- 1020 × 1370 About: 소개 문단의 계산된 스타일이 `word-break: keep-all`, `overflow-wrap: break-word`인지 확인했다.
- 1020 × 1370 Space: 클릭 안내 노출과 `/work/1` 이동을 확인했다.
- 1350 × 900 Space: 클릭 안내 노출과 상세 이동을 확인했다.
- 1351 × 900 Space: 커서 안내 노출, URL 유지, 미리보기 `aria-expanded=true`를 확인했다.
- 600 × 980 Space: 터치 안내 노출과 compact 지도 링크 이동을 확인했다.
- 1920·1020·600·400px에서 About, Work, Space의 계산된 위·아래 여백이 Profile 기준값보다 각각 `20px` 넓은지 브라우저로 교차 확인했다.
- 관련 About·Work·Space 테스트 44개를 통과했다.

## QA-005 — Message 장문 작성과 수신자 정보 위계

- 대상: `/message`, Web부터 Mobile까지 전체 반응형 구간
- 요청:
  - 카드에 수용할 메시지 최대 길이를 130자로 조정한다.
  - 현재 입력 글자 수와 제한을 작성 박스 좌측 하단에 표시한다.
  - 작성 영역과 입력 글자 크기는 기존 규격을 유지하고, 하단 포스트잇의 본문 밀도만 조정한다.
  - `To.`에서 실제 수신자를 선택하면 이름을 굵게 표시한다.
- 결정:
  - 브라우저의 `maxLength`와 상태 갱신 양쪽에서 130자 제한을 적용한다.
  - 카운터는 `현재 글자 수 / 130` 형식으로 표시하고 입력 영역의 설명과 연결한다.
  - 작성 영역의 크기는 포커스 전후 동일하게 유지한다.
  - 입력창 본문은 모바일 `9~12px`, 태블릿 `12~14px`, 웹 `16~20px`의 기존 유동 크기로 복원한다.
  - 포스트잇 본문은 웹 `16px`, 중간 화면 `14~16px`, 모바일 `10~12px`로 구분하고 카드 높이에 따라 최대 `7~11줄`을 노출한다.
  - 포스트잇의 고정 정사각형 크기와 `To`·`From` 위치는 유지하며, 남는 본문 공간을 더 사용한 뒤 긴 글만 말줄임 처리한다.
  - 기본값 `전체(모두)`는 regular, 실제 선택 수신자와 카드의 수신자 이름은 bold로 표시한다.
  - 수신자 이름과 하단 화살표는 동일한 flex 행에 두고, 필드 라벨에만 절대 위치를 적용한다.
  - 목업 데이터는 130자 이하의 메시지 8종을 서로 다른 줄바꿈으로 구성한다.
- 검증:
  - 400·600·1020·1350·1920px에서 130자를 입력해 카운터, 입력 제한, 본문 수용 여부를 확인했다.
  - 모든 규격에서 포커스 전후 작성 영역과 입력 글자 크기가 유지되고 카운터와 전송 버튼이 본문과 겹치지 않았다.
  - 하단 포스트잇은 130자 목업에서도 `From` 영역을 침범하지 않고 화면별 최대 줄 수에서 말줄임 처리되는지 확인했다.
  - 모든 규격에서 수신자 이름과 화살표의 계산된 위치가 `static`이며 선택 영역 안에서 수직 중앙 정렬되는 것을 확인했다.
  - 실제 수신자 선택 후 계산된 글꼴 굵기가 `700`으로 변경되는 것을 확인했다.

## QA-006 — Message 포스트잇 교차 배치

- 대상: `/message`, Web부터 Mobile까지 전체 반응형 구간
- 요청: 포스트잇 목록을 첨부 시안처럼 열마다 위아래가 교차하는 리듬으로 배치한다.
- 결정:
  - 4열 Web에서는 2·4번째 열을 아래로 이동한다.
  - 3열 Web > Tab에서는 가운데 열을 아래로 이동한다.
  - 2열 Tab·Mobile에서는 두 번째 열을 아래로 이동한다.
  - 이동량은 카드와 화면 크기에 맞춰 `clamp()`로 보간하고, 같은 크기의 목록 하단 여백을 확보한다.
- 검증:
  - 400px에서 첫 번째 열보다 두 번째 열이 아래에서 시작하며 행간이 유지되는 것을 캡처로 확인했다.
  - 구조 테스트에서 4열·3열·2열별 선택자와 하단 보정 공간이 모두 존재하는지 확인했다.

## QA-007 — Message 포스트잇 시작 위치 상향

- 대상: `/message`, Web부터 Mobile까지 전체 반응형 구간
- 요청: 장식 에셋 아래의 빈 공간을 줄이고 첫 포스트잇을 에셋 바로 아래에서 시작한다.
- 결정:
  - 목록의 문서 흐름은 유지한 채 컨테이너에 음수 `margin-top`을 적용해 뒤따르는 콘텐츠도 함께 당긴다.
  - Web·Web > Tab은 `192~240px`, Tab은 `48~96px`, Mobile은 `32~48px` 범위에서 화면 폭에 따라 상향량을 보간한다.
  - 기존 포스트잇 교차 열 이동량과 장식 에셋의 레이어 순서는 변경하지 않는다.
- 검증:
  - 1280px에서 보이는 장식 에셋 하단과 첫 카드 사이 간격이 약 `256px`에서 `28px`로 줄어든 것을 확인했다.
  - 400px에서 장식 에셋과 첫 카드 사이가 약 `11px`로 이어지고, 첫 번째·두 번째 열의 교차 배치가 유지되는 것을 캡처로 확인했다.

## QA-008 — Message 임의 화면 폭의 카드 텍스트 넘침

- 대상: `/message`, 400~1920px 사이의 고정 시안 외 중간 화면 폭
- 재현: 브라우저 폭을 연속해서 줄일 때 600px·1020px·1350px 인접 구간에서 카드 폭보다 글자와 안쪽 여백이 크게 남아 본문이 `From` 영역까지 내려왔다.
- 원인:
  - 600px에서 601px로 바뀔 때 본문은 `12px → 14px`, 라벨은 `12px → 16px`, 세로 여백은 약 `17px → 32px`로 동시에 증가했다.
  - 1020px 직후 3열, 1350px 직후 4열로 전환되어 각 카드가 130자 메시지를 담을 폭을 확보하지 못했다.
- 결정:
  - 카드에 `container-type: inline-size`를 적용하고 글자 크기·문단 간격을 실제 카드 너비 기준으로 보간한다.
  - 카드 안쪽 여백은 카드 폭의 비율을 사용하되 디자인 기준 최소·최대값을 제한한다.
  - 카드 콘텐츠 폭이 `18rem`, `13rem` 아래로 내려가면 본문 노출 줄 수를 각각 9줄, 7줄로 줄여 하단 발신자 영역을 보장한다.
  - 2열은 1149px, 3열은 1599px까지 유지하고 1600px 이상에서 4열로 전환한다.
- 검증:
  - `400, 500, 599, 601, 700, 820, 900, 1020, 1021, 1100, 1149, 1150, 1280, 1350, 1351, 1500, 1599, 1600, 1800, 1920px`에서 카드 8개씩 검사했다.
  - 총 160개 표본 모두에서 콘텐츠 넘침, 본문과 `From` 겹침, `From`의 카드 이탈이 발생하지 않았다.
  - 601px·1150px·1600px 경계 화면을 캡처해 열 전환과 교차 배치가 유지되는 것을 확인했다.

## QA-009 — Message 모바일 포스트잇 글자 축소

- 대상: `/message`, 320~600px 모바일 구간
- 요청: 모바일에서 포스트잇 본문이 빽빽하게 보이지 않도록 글자 크기를 한 단계 더 줄인다.
- 결정:
  - 본문은 카드 콘텐츠 폭에 따라 `8.5~10px`로 보간한다.
  - `To/From`은 위계를 유지하면서 `9~10.5px`로 보간한다.
  - 본문과 라벨 사이 간격도 `6~8px`로 함께 줄여 카드 내부 비율을 유지한다.
- 검증:
  - `320, 360, 375, 390, 400, 430, 480, 520, 560, 600px`에서 카드 8개씩 검사했다.
  - 총 80개 표본에서 본문과 `From` 겹침, 콘텐츠 넘침, 발신자 카드 이탈이 발생하지 않았다.
  - 400px 화면 캡처에서 축소된 본문과 하단 발신자 여백을 확인했다.

## QA-010 — Message 반복 줄바꿈에 의한 세로 넘침 방지

- 대상: `/message`, 메시지 작성 영역과 새로 생성되는 포스트잇
- 문제: 줄바꿈도 문자열 길이에는 1자로 계산되지만 화면에서는 새로운 한 줄의 높이를 차지하므로, 130자 제한만으로는 포스트잇의 세로 공간을 보호할 수 없다.
- 결정:
  - 메시지 본문은 기존과 동일하게 최대 130자로 제한한다.
  - 가장 좁은 모바일 포스트잇에서 한 줄이 차지하는 공간을 기준으로 줄바꿈 1회를 20자 사용량으로 계산한다.
  - 일반 문자는 1자, 줄바꿈은 20자를 사용한 것으로 합산해 총 사용량이 130을 넘지 않도록 입력을 정규화한다.
  - 남은 사용량이 한 줄보다 작을 때 추가한 줄바꿈은 공백으로 바꿔 뒤의 내용을 삭제하지 않고 같은 문단에서 이어지게 한다.
  - 서로 다른 운영체제의 줄바꿈 문자는 `\n`으로 통일한다.
  - 작성 영역 좌측 하단에는 별도의 줄바꿈 안내 없이 환산된 `현재 사용량 / 130`만 표시한다.
  - 카드의 화면별 글자 크기와 `line-clamp`는 예외적으로 긴 단어와 좁은 중간 화면을 막는 최종 안전장치로 유지한다.
- 검증:
  - 운영체제별 줄바꿈 정규화, 줄바꿈의 20자 환산, 최대 130 사용량 제한을 입력 규칙 테스트로 확인했다.
  - 400 × 880 화면에서 반복 줄바꿈을 입력했을 때 카운터에는 줄바꿈 문구 없이 환산된 사용량만 표시되는지 확인했다.
  - 동일한 메시지를 실제 포스트잇으로 추가해 본문 overflow, 본문과 `From` 겹침, `From`의 카드 이탈이 모두 발생하지 않는지 확인했다.

## QA-011 — Profile 상세 프로젝트 이미지 비율 통일

- 대상: `/profile/[id]`, Work 프로젝트 대표 이미지
- 재현: 1350px 이하에서는 이미지 컨테이너가 `1.8:1`이지만, 1350px보다 큰 화면에서는 높이가 `900px`로 고정되어 화면 폭에 따라 보이는 이미지 비율과 잘림 범위가 달라졌다.
- 결정:
  - 프로젝트 이미지 컨테이너에 전 반응형 구간 공통 `aspect-ratio: 1.8`을 적용한다.
  - 고정 높이를 제거하고 너비를 기준으로 높이가 계산되게 해 원본 3240 × 1800 이미지와 같은 비율을 유지한다.
  - 이미지는 기존 `object-fit: cover`를 유지해 이후 다른 원본이 연결되어도 컨테이너 비율과 레이아웃은 변하지 않게 한다.
- 검증:
  - `400, 600, 1020, 1350, 1351, 1500, 1920px`에서 컨테이너와 렌더링 이미지의 너비/높이 비율이 모두 `1.8`인지 브라우저에서 확인했다.
  - 1350→1351px 경계에서 컨테이너가 `1250 × 694.44px`에서 `1251 × 695px`로 연속적으로 변해 급격한 이미지 잘림 전환이 발생하지 않았다.
  - 1920px에서는 디자인 원본 기준인 약 `1620 × 900px` 크기를 유지했다.

## QA-012 — About 전시 장소명 SPACE 9 통일

- 원본 QA: `QA-001`, `QA-003`
- 대상: `/about`, Web·Web > Tab·Tab·Tab > Mobile·Mobile 5개 반응형 화면
- 요청: 오프라인 전시 정보의 장소명을 `SPACE 9`으로 통일한다.
- 결정:
  - 822px 이상에서 사용하는 오프라인 전시 통합 이미지를 최신 Figma 노드 `2287:18942` export로 교체한다.
  - 821px 이하의 실제 HTML 제목과 주소도 `경북대학교 SPACE 9`으로 변경한다.
  - 이미지 대체 텍스트에도 같은 표기를 적용한다.
- 검증:
  - `1920, 1350, 1020, 600, 400px`에서 `경북대학교 SPACE 9` 문구를 확인했다.
  - 822px 이상은 최신 Figma 통합 이미지, 821px 이하는 반응형 HTML 정보가 노출되는 기존 구조를 유지했다.

## QA-013 — Space 아카이브 소개 띄어쓰기

- 원본 QA: `QA-002`
- 대상: `/space`, 전체 반응형 화면
- 요청: `아카이브 입니다.`를 `아카이브입니다.`로 수정한다.
- 결정: 공통 Space 페이지의 소개 문구 원본을 `Ignite의 모든 순간을 담은 아카이브입니다.`로 변경한다.
- 검증: 구조 테스트와 브라우저 렌더링에서 잘못된 띄어쓰기가 더 이상 남지 않았음을 확인했다.

## QA-014 — 전시 종료일 10월 30일 통일

- 원본 QA: `QA-004`
- 대상: About 오프라인 전시 정보와 모든 페이지의 공통 푸터
- 요청: 종료일을 `10.30(금)`으로 변경한다.
- 결정:
  - About 최신 Figma export와 모바일 HTML에 `2026.10.20(화) — 10.30(금)`을 사용한다.
  - 공통 푸터에는 `2026.10.20 TUE — 2026.10.30 FRI`를 사용한다.
- 검증: 다섯 반응형 규격의 About 정보와 공통 푸터 원본을 교차 확인했다.

## QA-015 — Profile 태블릿 레이아웃 조기 전환

- 원본 QA: `QA-005`
- 대상: `/profile`, 1020px 초과 1100px 이하 구간
- 원인: 4열 Web > Tab 카드가 1020px 직전까지 유지되어 hover 이름 패널의 한글·영문 조합이 카드 폭을 넘었다.
- 결정:
  - Profile 전용 3열 레이아웃 전환점을 `1020px`에서 `1100px`로 올린다.
  - 같은 경계에서 hover 외곽선도 Web > Tab 에셋에서 Tab 에셋으로 전환한다.
  - 카드 이미지의 Next.js `sizes` 힌트도 새 열 수와 경계에 맞춘다.
  - 헤더·푸터 등 전역 `1020px` 반응형 경계는 변경하지 않는다.
- 검증:
  - `1101px`에서 4열·Web > Tab 프레임이 유지되는지 확인했다.
  - `1100px`에서 3열·Tab 프레임으로 동시에 전환되는지 확인했다.
  - Profile 관련 회귀 테스트에서 새 `68.75rem` 경계와 이미지 크기 힌트를 검증했다.

## QA-016 — About 지도 내부 장소명 SPACE 9 통일

- 원본 QA: `QA-001` (`443fb205-772d-4dc8-b759-314a9fc4c5c9`)
- 대상: `/about`, Web·Web > Tab·Tab·Tab > Mobile·Mobile 5개 반응형 화면
- 문제: 오프라인 전시 정보 텍스트는 `SPACE 9`으로 변경됐지만, 지도 래스터 이미지 안의 노란 위치 라벨에는 기존 `스페이스9` 표기가 남아 있었다.
- 결정:
  - 지도 이미지의 원본 비율 좌표를 기준으로 기존 노란 라벨과 정확히 겹치는 HTML 텍스트 레이어를 추가한다.
  - 넓은 화면과 잘라 쓰는 좁은 화면이 동일한 라벨 컴포넌트와 상대 좌표를 사용하도록 구성한다.
  - 라벨 문구를 `경북대학교 대강당 / SPACE 9`으로 통일하고, 이미지 크기에 따라 글자도 함께 비례하도록 컨테이너 쿼리 단위를 사용한다.
- 검증:
  - 넓은 화면과 좁은 화면에서 각각 라벨 레이어가 렌더링되는 구조 테스트를 추가했다.
  - 1920px 화면의 오프라인 전시 지도에서 기존 표기가 노출되지 않고 `SPACE 9`만 보이는지 브라우저로 확인했다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-017 — 공통 모바일 푸터 Figma 규격 반영

- 대상: About·Work·Profile·Space·Message 공통 푸터, Mobile 400px 화면
- Figma 기준: `1157:22590`, 400 × 172px
- 결정:
  - 푸터 안쪽 여백을 상하 25px·좌우 10px로 유지하고, 콘텐츠를 109px·233px 두 열과 38px 간격으로 배치한다.
  - 로고를 30.325 × 31.56px로 표시하고 좌측 전시 문구를 Figma와 동일한 다섯 줄로 고정한다.
  - 일정은 12px, Instagram과 저작권은 11px·`#CCC`로 표시하며 모든 화면에서 Instagram 링크 밑줄을 제거한다.
  - 저작권을 두 줄로 고정해 우측 열 밖으로 이탈하지 않게 한다.
  - 400px 미만은 Figma 원본이 없는 적응 구간으로 구분하고, 400px의 배치를 유지하면서 열 간격과 우측 정보 글자를 연속 축소한다.
- 검증:
  - 400px에서 콘텐츠 좌표 `x: 10, y: 25`, 크기 380 × 약 122px를 확인했다.
  - 좌측 영역은 109px, 우측 영역은 233px, 로고는 브라우저 반올림 기준 30.31 × 31.55px로 렌더링됐다.
  - About·Work·Profile·Space·Message 5개 화면에서 푸터 및 문서의 가로 넘침과 Next.js 오류 오버레이가 없음을 확인했다.
  - 320·360·400·480·600px 적응성 검사에서도 푸터 가로 넘침이 발생하지 않았다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-018 — About 포스터 최종 자산 교체

- 대상: `/about`, 소개 영역 포스터(Web·Tab·Mobile)
- 요청: About에 사용되는 기존 포스터를 전달받은 최종 포스터 이미지로 교체한다.
- 결정:
  - `public/assets/figma/about/poster.png`를 최종 포스터 원본 `1808 × 2560px`으로 교체한다.
  - 기존 포스터와 같은 세로 비율을 유지하므로 Web·Tab·Mobile의 컨테이너, `object-fit: cover`, 위치·반응형 규칙은 그대로 재사용한다.
  - 두 렌더링 경로(`posterWide`, `posterNarrow`) 모두 새 원본 크기를 Next Image 메타데이터에 반영한다.
- 검증:
  - 400·600·1020·1350·1920px에서 새 이미지의 natural size가 `1808 × 2560`으로 로드되는지 확인했다.
  - 활성 포스터의 비율이 모든 구간에서 유지되고, 문서 가로 넘침과 Next.js 오류 오버레이가 없음을 확인했다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-019 — About Instagram 링크 밑줄 제거 재반영

- 대상: `/about`, 소개 영역 Instagram 링크(Web·Web > Tab·Tab·Tab > Mobile·Mobile)
- 문제: 공통 링크 초기화가 적용되어도 About 전용 스타일에서 `text-decoration: underline`이 다시 선언되어 Instagram 링크 아래 밑줄이 남아 있었다.
- 결정:
  - About 소개 링크의 전용 스타일을 `text-decoration: none`으로 명시해 전 반응형 구간에서 동일하게 밑줄을 제거한다.
  - 다른 링크의 hover·focus 동작은 변경하지 않고 About Instagram 링크에만 범위를 제한한다.
- 검증:
  - About 스타일 회귀 테스트에서 링크 선언이 `text-decoration: none`인지 확인했다.
  - 400·600·1020·1350·1920px 반응형 구간에서 동일한 About 링크 스타일이 적용되는 구조를 확인했다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-020 — 히어로 모션 멈춤 UI 제거

- 대상: `/`, 히어로 모션 영역(Web·Tab·Mobile)
- 요청: 히어로 화면에 노출되던 모션 멈춤/재생 버튼을 제거한다.
- 결정:
  - HeroMotion 컴포넌트에서 일시정지 상태와 토글 버튼을 제거한다.
  - 모션은 기존처럼 화면 노출 여부와 `prefers-reduced-motion` 설정에 따라 자동 재생·정지한다.
  - 화면에 보이지 않는 모션 제어 UI와 관련 CSS를 함께 삭제한다.
- 검증:
  - 히어로 모션 회귀 테스트에서 토글 UI·상태 속성이 제거됐는지 확인했다.
  - 히어로 애니메이션의 화면 노출 기반 `data-running` 제어가 유지되는지 확인했다.
  - 메인 상호작용 테스트 5개와 ESLint를 통과했다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-021 — 노트북 폭 공통 헤더 비율 보정

- 원본 QA: `QA-001` (`aa28ecb1-e35a-4bee-9f2e-c6b2e2130b23`)
- 대상: About·Work·Profile·Space·Message 공통 헤더, 1350px 이상 데스크톱 구간
- 문제: 14인치 MacBook처럼 1350~1920px 사이 폭에서는 브랜드 제목이 20px로 고정되고 메뉴 크기도 상대적으로 커, 1920px 기준보다 조밀하고 크게 보였다.
- 결정:
  - 헤더 전용 좌우 여백을 1350px의 75px에서 1920px의 150px까지 연속 보간한다.
  - 브랜드 제목은 18→20px, 메뉴 글자는 22→26px, 메뉴 한 칸 너비는 110→130px로 연속 보간한다.
  - 헤더 높이·로고 크기와 1350px 이하 햄버거 메뉴 규격은 유지해 기존 모바일·태블릿 구조에 영향을 주지 않는다.
- 검증:
  - 1350~1920px 헤더 토큰의 보간식과 하한·상한을 회귀 테스트로 확인한다.
  - 공통 헤더 변경이므로 5개 페이지에 동일하게 적용된다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-022 — 500px 이하 메인 히어로 하단 경계 제거

- 대상: `/`, 500px 이하 Mobile 히어로
- 문제: 모바일 히어로 SVG의 그라데이션 끝 색과 최소 화면 높이로 남는 영역의 배경색이 달라 하단에 분리선이 보였다.
- 결정:
  - 600px 이하의 `.main-hero` 배경을 모바일 SVG의 끝 색 `#001A27`로 맞춘다.
  - SVG보다 히어로 컨테이너가 높아지는 화면에서도 같은 색을 연장해 시각적 경계를 없앤다.
- 검증:
  - 모바일 구간의 히어로 하단 배경값을 회귀 테스트로 확인한다.
  - 기존 1020px·1350px·1920px 하단 배경 보정은 유지한다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-023 — Message 입력·포스트잇 스크롤 모션 추가

- 대상: `/message`, 작성 폼과 메시지 포스트잇 목록
- 요청: 첫 화면의 메시지 입력 박스와 아래 포스트잇이 스크롤에 맞춰 부드럽게 나타나고 사라지는 인터랙션을 제공한다.
- 결정:
  - 작성 폼은 화면 진입 시 20px 이내의 짧은 세로 이동과 투명도 전환으로 나타난다.
  - 포스트잇은 기존의 지그재그 배치를 유지한 채, 각 카드가 화면 진입·이탈 시 24px 이내에서 자연스럽게 나타나고 사라진다.
  - IntersectionObserver로 가시 상태를 추적해 위·아래 재진입 때도 반복하고, `prefers-reduced-motion`에서는 정적 레이아웃을 유지한다.
- 검증:
  - 메시지 구조 테스트에서 관찰 대상, 가시 상태 속성, reduced-motion 처리와 전환 규칙을 확인한다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-024 — Message 포스트잇 배치와 모션 속성 분리

- 대상: `/message`, 포스트잇 목록(Web·Web > Tab·Tab·Tab > Mobile·Mobile)
- 문제: 카드 지그재그 배치와 스크롤 모션이 모두 `transform`을 사용해, 모션이 활성화되면 기존 열별 세로 위치가 덮어써졌다.
- 결정:
  - 기존 `transform: translateY(...)`는 카드의 지그재그 레이아웃 전용으로 유지한다.
  - 모션은 CSS 개별 변환 속성인 `translate`와 `opacity`만 사용해 배치 계산과 독립적으로 합성한다.
  - 작성 폼도 기존의 `translateX(-50%)` 가운데 정렬을 유지한 채 세로 모션만 `translate`로 더한다.
- 검증:
  - 메시지 스타일 테스트에서 배치용 임시 CSS 변수 없이 `translate` 전환만 사용하는지 확인한다.
  - 모든 반응형 열 구성에서 기존 `nth-child` 지그재그 배치 규칙이 그대로 적용된다.
- 상태: 수정 완료, 디자이너 검토 요청 대기

## QA-025 — Message 작성 폼 모션 제외

- 대상: `/message`, 상단 메시지 작성 폼
- 결정: 작성 폼은 첫 화면에서 즉시 읽고 입력하는 핵심 행동 요소이므로 스크롤 관찰·등장 모션 대상에서 제외한다.
- 유지: 하단 포스트잇 카드만 `opacity`와 `translate`를 이용한 완만한 스크롤 전환을 유지한다.
- 상태: 수정 완료, 디자이너 검토 요청 대기
