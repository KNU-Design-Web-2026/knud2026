# KNUD 2026 색면 폭발 모션 기준

## 최종 승인 — 꼬리 원형 유지와 장면 반응 보강

디자이너 검토를 거친 현재 메인 화면을 최종안으로 사용한다. 꼬리 경로와 실루엣은 원본 그대로 유지하고, 사자 전체에는 폭발 순간 최대 4px의 짧은 반동과 대기 중 1px 이내의 무게 이동만 적용한다. 스프레이 본체도 같은 타임라인에서 최대 5px·2.2도의 짧은 반응을 보이며 새로운 마우스 이벤트는 추가하지 않는다. 모바일에서는 이동·회전량을 절반 수준으로 낮춘다.

눈은 5.6초 주기 중 긴 정지 뒤 두 번 깜빡여 정적인 인상을 줄인다. IGNITE 확대, 색면 연쇄 폭발, 후속 파티클은 기존 승인값을 유지한다. `prefers-reduced-motion`에서는 장식 모션을 모두 제거한다. 이 항목이 아래의 꼬리·대기 동작 검토 기록보다 우선한다.

## 최신 검토 — 꼬리 고정, IGNITE 임팩트

이 항목이 아래 꼬리 움직임 검토안을 대체한다. 꼬리는 원본 SVG 경로로 고정하고 생성물에서 변형 포즈를 제거한다. IGNITE는 3.8초 주기 중 1.05초부터 예비 수축, 1.33초에 최대 확대, 1.88초에 원형 복귀한다. 데스크톱 배율은 0.985~1.05, 모바일은 0.99~1.025이며 회전·반복 반동·마우스 이벤트를 추가하지 않는다. 모션 감소 설정에서는 정적 원본으로 표시한다. 기존 파티클·눈 깜빡임·스프레이는 유지한다.

## 최신 이미지 피드백 — 곡선이 펴졌다 말리는 꼬리

첨부된 연속 동작 이미지를 기준으로 작은 회전을 곡선 변형으로 교체한다. 원본 외곽선·노란 면·줄무늬·끝 불꽃을 같은 중심선 좌표에 묶어, 말린 모양에서 바깥으로 펼쳐졌다 복귀하는 13개 자세를 생성한다. 코사인 궤적을 24구간으로 연결하고 중간 자세의 개별 easing과 정지 구간을 제거한다. 데스크톱 3.6초, 모바일 4초 주기로 재생한다. 이 규격은 아래의 4.8초 회전 검토안을 대체한다.

몸통에 가려진 시작 구간은 고정한다. 자세 사이에서 각도가 뒤집히거나 꼬리가 접히지 않도록 중심선 방향을 연속적으로 보간한다. 400·600·1920 원본 프레임은 가로 최대 동작을 줄여 꼬리 끝에 여유를 남긴다. 원본 SVG 파일은 보존하며 실행 중 포인터 이벤트나 기하 계산 루프는 추가하지 않는다. 모션 감소 상태와 CSS 경로 애니메이션 미지원 환경에서는 원본 꼬리를 표시한다.

## 최신 검토 반영 — 꼬리 흔들림과 대기 동작

사용자 요청으로 아래의 꼬리 고정 규칙을 변경한다. 원본 꼬리의 길이와 형태는 유지하되 몸통에 가려진 접점을 회전축으로 왕복 4.8초, 좌우 2.5도 흔든다. 600px 이하에서는 1.6도로 줄인다. 꼬리 끝 불꽃도 원본 꼬리와 함께 움직인다.

폭발 사이에는 세 색면과 해당 파편 그룹이 5.1·5.8·6.7초의 독립 주기로 최대 3px 이동한다. 모바일에서는 1.5px로 제한한다. 사자 몸통과 IGNITE는 고정하고, 마우스 이벤트는 스프레이만 사용한다. 모션 감소 설정에서는 꼬리와 색면도 정지한다.

## 시각 전제

원본 포스터의 파랑·빨강·노랑 폭발 색면이 인쇄된 그래픽에서 순간적으로 튀어나오는 장면으로 표현한다

## 유지와 제거

### 유지

- 기존 SVG의 사자, 꼬리, 꼬리 끝 불꽃, IGNITE 로고, 세 개 폭발 색면
- 현재 색면의 압축 후 확장되는 기본 폭발 방향
- 눈 깜박임과 화면 밖 재생 정지
- KNUD 원본 팔레트 `#F21C1C`, `#FCD519`, `#41C9F9`, `#FD9519`, 검정

### 제거

- 꼬리를 따라 이동하는 불꽃
- 꼬리 마스크, 소진, 숨김, 복구 애니메이션
- 꼬리 접점에서 발생하는 열기·불씨·파편
- 사자 몸통의 큰 점프와 찌그러짐
- 로고의 반복 호흡과 과한 반동
- 폭발 뒤 다시 반대로 튀는 오버슈트

## 모션 구조

전체 주기는 **3.8초**로 통일한다

| 구간 | 시간 | 표현 |
| --- | ---: | --- |
| 대기 | 0–1.05초 | 원본 포스터 유지, 눈 깜박임만 별도 저주기로 허용 |
| 준비 | 1.05–1.18초 | 색면을 0.90–0.94배로 짧게 압축, 회전은 2도 이내 |
| 폭발 | 1.18–1.42초 | 색면을 최대 크기로 한 방향 확장, 각 중심에서 파티클 방사 |
| 정착 | 1.42–1.88초 | 1.02배를 거쳐 원본 크기로 감속 복귀, 역방향 재확대 없음 |
| 여운 | 1.88–3.8초 | 원본 상태 유지 후 다음 주기 연결 |

색면은 파랑·빨강 → 노랑 → 주황 순서로 130ms 간격을 두고 폭발한다. 각 색면의 첫 파편 뒤 150ms에 작은 후속 파편이 다른 각도로 1.45배 더 멀리 퍼진다. 전체 반복 주기는 3.8초를 유지하며, 마지막 파편은 약 2.05초에 사라진다. 별도 포인터·클릭·터치 이벤트는 추가하지 않고 마우스 입력은 기존 스프레이에만 사용한다

## 파티클

- 발생 위치: `hero-burst-0`, `hero-burst-1`, `hero-burst-2` 각각의 중심
- 형태: 원형 점이나 일반 컨페티가 아닌 원본 폭발 외곽에서 잘라낸 듯한 삼각형·사다리꼴 조각
- 색상: 발생한 색면의 주조색과 인접 색상만 사용
- 개수: 데스크톱 색면당 첫 파편 7개와 후속 4개, 총 33개 / 모바일·세로 태블릿은 첫 파편 4개와 후속 2개, 총 18개
- 시간: 340–460ms 사이에 빠르게 나타나고 사라짐
- 거리: 데스크톱 34–86 SVG 좌표 단위 / 모바일은 데스크톱의 55%
- 방향: 중심에서 방사하되 사자 얼굴과 내비게이션 텍스트 쪽으로 집중되지 않도록 각도 분산
- 크기: 시작 0.65배 → 최대 1배 → 종료 0.25배
- 블러, 그림자, 네온 광원, 화면 전체 플래시는 사용하지 않음

## 색면별 반응

- 파랑·빨강 외곽 색면: 가장 넓게 확장하며 파란 면과 빨간 외곽에서 두 색 파편을 함께 방사
- 노란 색면: 확장률은 낮추고 작은 노란·주황 파편을 빠르게 방사
- 주황·빨강 색면: 세 번째 독립 배경 도형이며 확대율을 가장 작게 제한
- 꼬리 경로: 위치와 형태를 고정해 사자 실루엣을 유지
- 사자와 스프레이: 폭발 순간에만 작은 반동을 적용하고 대기 중 이동은 1px 이내로 제한
- IGNITE 로고: 짧은 수축·확대 후 반동 없이 원형으로 복귀

## 반응형 규칙

- 601px 이상: 색면 최대 1.32배, 회전 4도 이하, 데스크톱 파티클 규격 사용
- 600px 이하: 색면 최대 1.18배, 회전 2도 이하, 파티클 수와 거리를 축소
- 세로형 태블릿: 모바일 파티클 밀도를 사용하되 색면 확대율은 1.22배까지 허용
- 모든 구간에서 SVG 외곽, 헤더, 사자 얼굴과 겹치는 파편은 생성 단계에서 방향을 조정

## 구현 원칙

- 원본 SVG 파일은 수정하지 않고 생성 스크립트가 클래스와 파티클 그룹을 삽입
- 파티클은 CSS transform·opacity만 사용하고 Canvas·추가 RAF 루프는 만들지 않음
- 다섯 반응형 SVG가 같은 문서에 있으므로 ID를 프레임별로 네임스페이스 처리
- `prefers-reduced-motion: reduce`에서는 모든 장식 모션 제거
- 스프레이 Canvas, 입력 좌표, 수명, 색상 로직은 변경하지 않음

## 피해야 할 표현

- 꼬리 형태 변경 또는 꼬리가 사라지는 장면
- 오래 타들어가는 서사와 폭발 전 긴 대기
- 별이 말랑하게 늘었다 줄어드는 젤리 모션
- 모든 파티클이 같은 각도·거리로 퍼지는 방사형 프리셋
- 원본에 없는 보라색, 그라데이션, 글로우 효과
- 사자와 IGNITE 로고까지 동시에 크게 흔드는 화면 전체 충격

## 사전 QA

- 세 폭발 색면의 실제 중심과 안전한 파티클 방향 확인
- 꼬리 관련 마스크·이동 불꽃·접점 파편 제거 범위 확인
- 스프레이 레이어와 SVG 파티클의 z-index 확인
- 모바일 파편이 화면 밖에서 잘려 빈 동작으로 보이지 않는지 확인

## 구현 후 QA

- 400·600·1020·1350·1920px에서 폭발 전·최대·정착 시점 캡처 비교
- 꼬리 SVG 경로가 전 구간 변하지 않는지 자동 검사
- 사자·스프레이가 폭발 시점에 반응하고 모바일에서 이동량이 줄어드는지 확인
- 세 색면 모두 파티클을 생성하는지 검사
- 전체 주기 3.8초와 첫 폭발 1.2초 이내 여부 검사
- 가로 넘침, 콘솔 오류, 화면 밖 재생 정지, 모션 감소 상태 검사
- 스프레이 빠른 입력과 동시에 재생해 프레임 저하 여부 별도 측정
